{
  "name": "express-mongo-vercel",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "nodemon api/index.js",
    "build": "echo 'No build step required for Vercel'"
  },
  "engines": {
    "node": ">=18.x <=20.x"
  },
  "dependencies": {
    "express": "^4.19.2",
    "mongoose": "^8.7.0",
    "serverless-http": "^3.2.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.7"
  },
  "vercel": {
    "functions": {
      "api/**/*": {
        "memory": 1024,
        "maxDuration": 30
      }
    }
  }
}
